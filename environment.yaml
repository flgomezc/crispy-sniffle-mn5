name: hpc-inference
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - vllm>=0.6.0
      - datasets>=2.20.0
      - transformers>=4.45.0
      - scikit-learn>=1.5.0
      - pandas>=2.2.0
      - tqdm
